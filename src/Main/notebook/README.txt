1. Перенёс всю логику из слоя dao, затем удалив его;
2. Реализовал метод delete;
3. Оптимизировал метод-заглушку findById (заглушка, так как он возвращал empty);
4. Оптимизировал клиентский код. Убрал всё в отдельный метод в слой util. Не уверен, что ему там место, но в слое view
ему делать нечего, так как не является обработкой команд/данных как для контроллера, так и для вайвера(view).
5. Мне кажется, что метод CreateUser может находиться в слое view, так как он получает и обрабатывает данные для
контроллера, после передаёт их в контроллер, а от него в модель. По структуре model-view-controller так можно делать поидее.

!!! P.S. Пожалуйста, подскажите почему метод delete перестал удалять, после оптимизации заглушки findById он перестал
!!! удалять. Я проверил по очередно обращения в методах, как мне кажется, всё должно работать, но НЕТ. Ответьте в письме
!!! на портале, где я ошибся. Заранее благодарю!
